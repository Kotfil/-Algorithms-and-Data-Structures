function knapsack(weights, values, W) {
    const n = weights.length;
    const dp = Array.from({ length: n + 1 }, () => Array(W + 1).fill(0));

    for (let i = 1; i <= n; i++) {
        for (let w = 0; w <= W; w++) {
            if (weights[i - 1] <= w) {
                dp[i][w] = Math.max(
                    dp[i - 1][w],
                    dp[i - 1][w - weights[i - 1]] + values[i - 1]
                );
            } else {
                dp[i][w] = dp[i - 1][w];
            }
        }
    }

    return dp[n][W];
}


Отлично, разбираем задачу **рюкзака пошагово**, с комментариями, **таблицей `dp`**, и логикой выбора предметов.

---

## 📦 Входные данные:

```js
const weights = [2, 1, 3];  // веса предметов
const values =  [4, 2, 3];  // ценности предметов
const W = 4;                // максимальный вес рюкзака
```

---

## 🔧 Инициализация `dp`-таблицы:

```js
const dp = Array.from({ length: n + 1 }, () => Array(W + 1).fill(0));
```

Получаем матрицу `dp[4][5]` (размер `[n+1][W+1]`):
где `dp[i][w]` — максимальная ценность при использовании **первых i предметов** при **весе w**

---

## 🔁 Проходим по предметам и весу:

```js
for (let i = 1; i <= n; i++) {
    for (let w = 0; w <= W; w++) {
```

### 🔍 Разбор строк `dp[i][w]` по шагам:

---

### ▶ i = 1 (первый предмет, вес = 2, ценность = 4)

| w (вес) | Условие `2 <= w`? | dp\[1]\[w]                                               |
| ------- | ----------------- | -------------------------------------------------------- |
| 0       | ❌ нет             | `dp[1][0] = dp[0][0] = 0`                                |
| 1       | ❌ нет             | `dp[1][1] = dp[0][1] = 0`                                |
| 2       | ✅ да              | `dp[1][2] = max(dp[0][2], dp[0][0] + 4) = max(0, 4) = 4` |
| 3       | ✅ да              | `dp[1][3] = max(dp[0][3], dp[0][1] + 4) = max(0, 4) = 4` |
| 4       | ✅ да              | `dp[1][4] = max(dp[0][4], dp[0][2] + 4) = max(0, 4) = 4` |

#### 👉 dp после 1-го предмета:

```
[0, 0, 0, 0, 0]
[0, 0, 4, 4, 4]
```

---

### ▶ i = 2 (второй предмет, вес = 1, ценность = 2)

| w | Условие `1 <= w`? | dp\[2]\[w]                                                 |
| - | ----------------- | ---------------------------------------------------------- |
| 0 | ❌ нет             | `dp[2][0] = dp[1][0] = 0`                                  |
| 1 | ✅ да              | `dp[2][1] = max(dp[1][1], dp[1][0] + 2) = max(0, 2) = 2`   |
| 2 | ✅ да              | `dp[2][2] = max(dp[1][2], dp[1][1] + 2) = max(4, 2) = 4`   |
| 3 | ✅ да              | `dp[2][3] = max(dp[1][3], dp[1][2] + 2) = max(4, 6) = 6` ✅ |
| 4 | ✅ да              | `dp[2][4] = max(dp[1][4], dp[1][3] + 2) = max(4, 6) = 6` ✅ |

#### 👉 dp после 2-го предмета:

```
[0, 0, 0, 0, 0]
[0, 0, 4, 4, 4]
[0, 2, 4, 6, 6]
```

---

### ▶ i = 3 (третий предмет, вес = 3, ценность = 3)

| w | Условие `3 <= w`? | dp\[3]\[w]                                               |
| - | ----------------- | -------------------------------------------------------- |
| 0 | ❌ нет             | `dp[3][0] = dp[2][0] = 0`                                |
| 1 | ❌ нет             | `dp[3][1] = dp[2][1] = 2`                                |
| 2 | ❌ нет             | `dp[3][2] = dp[2][2] = 4`                                |
| 3 | ✅ да              | `dp[3][3] = max(dp[2][3], dp[2][0] + 3) = max(6, 3) = 6` |
| 4 | ✅ да              | `dp[3][4] = max(dp[2][4], dp[2][1] + 3) = max(6, 5) = 6` |

#### 👉 dp после 3-го предмета:

```
[0, 0, 0, 0, 0]
[0, 0, 4, 4, 4]
[0, 2, 4, 6, 6]
[0, 2, 4, 6, 6]
```

---

## ✅ Ответ:

```js
console.log(dp[3][4]); // 6 — максимальная ценность
```

---

## 🔁 Восстановление выбранных предметов:

```js
function getSelectedItems(dp, weights, values, W) {
    const selected = [];
    let i = weights.length;
    let w = W;

    while (i > 0 && w > 0) {
        if (dp[i][w] !== dp[i - 1][w]) {
            selected.push(i - 1);
            w -= weights[i - 1];
        }
        i--;
    }

    return selected.reverse();
}
```

---

### ▶ Пошагово:

* `i = 3`, `w = 4`: `dp[3][4] === dp[2][4]` → ❌ не брали 3-й
* `i = 2`, `w = 4`: `dp[2][4] !== dp[1][4]` → ✅ взяли 2-й (`i-1 = 1`), `w = 4 - 1 = 3`
* `i = 1`, `w = 3`: `dp[1][3] !== dp[0][3]` → ✅ взяли 1-й (`i-1 = 0`), `w = 3 - 2 = 1`

→ выбранные индексы: `[1, 0]` → предметы 2 и 1

---

### ✅ Итог:

* **Максимальная ценность**: `6`
* **Взятые предметы**: `2-й (вес 1, ценность 2)` + `1-й (вес 2, ценность 4)`

Хочешь усложнённую версию или визуализацию?
